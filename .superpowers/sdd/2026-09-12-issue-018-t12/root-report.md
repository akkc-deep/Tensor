# Root implementation report — CORS and ordinary mock entry points

Spec: docs/task-designs/ISSUE-018-T12-design.md; scope sections CORS, ordinary suite boundaries/helper and three mock files. No production UI edits, source RANGE gate changes, package or real-account tests claimed.

Changes: SpaWebConfiguration adds Location exposure only, exact origin/security boundaries retained. ProductionWebConfigurationTest prior RED28 one failure, GREEN29 incl 202Location. Three-suite Playwright config, exact flow/resume params and reporters; seven ordinary specs serialize. New configurePackagedEnvironment helper validates current-user0600/non-symlink regular absolute JSON path, exact four-key/five-string records, prefixes/JDBC schema/defaults path; all validated before assigning selected five values, fixed error no secrets. Existing fixture rawJson/task/batch exports reused. ui-redesign mock adds task endpoints/strict metadata, task receipts and saved snapshots, defaults 34NEEDS/6UNSUPPORTED; ordinary matrix now submits once, gets persisted fixture detail/counters, returns via router. stock tests preserve normalized params and same submissionId on manual confirmation. Prior layout scenes lacked ts_code and pagination fixtures produced undefined past row8; fixed only fixtures. New screenshots in test cache, old ISSUE004 images restored; no user work overwritten.

Tests: CORS Maven command /tmp/issue018-t12-cors-{red,green}.log; GREEN29 failures/errors/skips0. Node helper RED missing module, GREEN19 /tmp/issue018-t12-env-{red,green}.log. Final three mock specs57/57 in2.5m /tmp/issue018-t12-mock-final.log, containing exact40 metadata+3stock+3task and remaining11 UI/query/layout/settings/keyboard/screenshots. Full Vue unit/build/Maven six gates pending final source integration. --list checks deferred while lifecycle owns reactor because npm ci rewrites node_modules. Existing code all staged, no commit. WORKSPACE user PRODUCT.md externally created remains untouched.

Please review source/task-spec compliance and quality. Do not rerun tests against same code; verify reported evidence and actual assertions. Report any exact missed behavior or regressions, with severity/path. The four packaged specs/Java lifecycle/fault tests are separate owners and not part of this scoped diff.
