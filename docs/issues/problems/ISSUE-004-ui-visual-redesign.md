# ISSUE-004：前端 UI 不美观，缺乏科技感

## 当前阶段

已解决（2026-09-07）。最终视觉设计已在正式前端实施，T01–T06全部完成；170项单测、构建、60项浏览器验收、独立规格/质量与视觉复审及最终项目审查均通过。用户明确的日期约束保持不变：单日期保持单日期，支持起始 / 结束的保留起始 / 结束。

设计依据：[数据工作台 UI 最终方案](../proposals/ISSUE-004-ui-visual-concepts.md) · [HTML 参考文件](../proposals/ISSUE-004-ui-visual-concepts.html)。

实施入口：[技术设计](../../task-designs/ISSUE-004-design.md) · [实施计划](../../superpowers/plans/2026-09-07-issue-004-ui-redesign.md) · [任务看板](../../task-handoffs/ISSUE-004/ISSUE-004-task-board.md)。

任务顺序为主题基础、侧栏 / 设置 / 状态保留、日期控件与原参数契约、下载页、查看页、正式前端回归与视觉验收。任务看板六项均为COMPLETED，全部关闭条件已有[正式验收记录与八张截图](../../verification/ISSUE-004-ui-redesign.md)。

## 问题与目标

现有前端的视觉表现需要整体提升。重新设计页面布局、信息层级、配色、字体、间距及组件状态，形成具有科技感的证券数据工作台，同时保留现有业务能力和后端接口。

## 最终方案

- 桌面使用固定宽度的左侧导航，包含数据下载、数据查看与设置入口。
- 下载页采用左侧配置、右侧结果的分栏布局；查看页采用上方筛选、下方表格与分页的布局。
- 默认冰川白配色，自定义主题色联动背景、侧栏、面板、边框、文字与操作色，布局与颜色独立。
- 主题颜色调整集中在“设置 → 外观与主题”，设置仅由前端管理，不与后端交互；往返设置与业务页保留日期、筛选条件、分页和操作状态。
- 主题偏好在当前浏览器保存，支持 HEX 校验、自动对比度调整和恢复默认配色；成功与错误保留各自语义色。
- 日期按接口元数据呈现：单日期使用一个控件，支持日期范围时使用独立的开始与结束控件。
- 统一待操作、加载、成功、空数据、错误、禁用及键盘焦点状态。
- 窄屏采用纵向布局，宽表格在内部滚动。

## 实施约束

- 前端位于 `control-plane`，沿用 Vue 3、Vite、Element Plus、现有 API 客户端和元数据驱动能力。
- 不修改后端接口路径、HTTP 方法、请求参数、响应结构、状态码、错误码及语义，不新增后端接口依赖。
- 保留数据源与接口选择、动态参数表单、下载结果、数据集筛选、表格展示、分页及重试能力。
- 下载按现有参数一次提交、一次请求，单日期不扩展为日期范围；查看日期沿用各数据集现有筛选契约。
- 正式页面使用现有接口提供的数据，HTML 参考文件中的样例值仅用于方案展示。
- 遵循最小代码原则，优先复用现有组件；实现中多次出现且职责相同的 UI 必须抽取为可复用组件，共用逻辑集中实现，各使用处统一接入，避免重复编写和过度抽象。

## 关闭条件

- 最终方案已在正式前端完成实施，主要页面具有统一、清晰且有科技感的视觉效果。
- 设置导航、整页主题联动、本地偏好保存、独立日期输入及各业务场景功能正常；设置操作无后端请求。
- 后端接口及契约保持不变，前端继续通过现有接口完成业务操作。
- 桌面和窄屏布局可用，文字、控件、宽表格与键盘焦点可辨识。
- 重复且职责相同的 UI / 逻辑已抽取并实际复用，验收记录列出共享实现及使用位置，原业务行为通过回归。
- 前端构建、相关回归与接口兼容验证通过，验收结果已记录。


## 验收结论

全部关闭条件通过。正式前端覆盖49个接口、851个业务字段、155列宽表、五种视口、四种主题和完整键盘流程；原请求、日期、精确数字及业务状态契约保持不变。独立审查无Critical或Important问题；一项仅影响未来测试输入维护的YAML解析器Minor已接受延期，现由 [ISSUE-016](ISSUE-016-ui-fixture-yaml-validation.md) 独立跟踪，本问题保持已解决。详细命令、结果、组件复用、截图与合成API范围见[验收记录](../../verification/ISSUE-004-ui-redesign.md)。
