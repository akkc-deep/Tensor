# ISSUE-018-T06 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** 实现 Tushare 区间策略、日期规划和 fina_mainbz SINGLE 参数纠正。

**Architecture:** 插件内两个文件拥有策略和完整日历校验；插件委托固定批量合同并复用已有客户端/context。任务接收只读取本地能力。

**Tech Stack:** Java 21、Maven、JUnit 5、RestClient、Node 24.15.0/npm 11.12.1、Playwright Chromium。

**Spec:** `docs/task-designs/ISSUE-018-T06-design.md`，全文为接口、逐项策略和行为矩阵的实施合同。

## Global Constraints

- 保持当前 `feat/download-by-date-range` 工作区，新文件加入 Git，不自动提交。
- 所有 34 生产策略 NEEDS_VERIFICATION / UNKNOWN；不执行真实 API 验收，不开放生产 RANGE。
- 仅新增两个生产文件，不增加依赖，不修改 T01 公共合同、T05 传输或其余 39 份 SINGLE YAML。
- 无 worker、新 HTTP、前端任务功能、自动重试或业务键调整。

### Task 1: 策略门面与日历

**Files:** 新增 `data-plane/tensor-plugin-tushare/src/main/java/com/akkc/tensor/plugin/tushare/batch/{TushareBatchPolicies,TushareTradeCalendar}.java`；同模块测试 `batch/{TushareBatchPoliciesTest,TushareTradeCalendarTest,TushareBatchDownloadTest}.java`。

**Interfaces:** 实现专属设计 Approach 的精确四个 public 门面方法、唯一 public 构造器和包内测试构造器。供 Task 2 委托消费；不编辑插件本体。

- [x] 独立列出 34 行/6项 SINGLE-only 预期，先运行 `mvn -f data-plane/pom.xml -pl tensor-plugin-tushare -am -Dtest=TushareBatchPoliciesTest -Dsurefire.failIfNoSpecifiedTests=false test` 观察缺失类红灯。
- [x] 按设计实现不可变注册、描述、四类纯参数转换、三种规划、响应范围和完整性判断；逐项行为先写失败测试，再实施。
- [x] 以受控时钟和真实客户端/mock HTTP 验证日历完整覆盖、阈值边界、日期轴、预约和错误传播；运行三项新增测试，自查全部矩阵。

### Task 2: 插件接线与 SINGLE 纠正

**Files:** 专属设计 Files 列出的插件、fina_mainbz.yaml、请求示例、插件/元数据/客户端/app 精确回归和三个 e2e 元数据来源；新增 `TushareBatchAvailabilityTest.java`。

**Interfaces:** TushareProPlugin 原三项 + 五项可选方法，四项委托门面，downloadBatch 复用 readiness/查找并调用三参数 execute；SINGLE fina_mainbz 为 ts_code + snapshot。

- [x] 先更新插件表面及 fina_mainbz 单股票合法/ann_date非法断言并观察失败，再实现最小委托及 YAML 修正。
- [x] 同步 40 请求示例与精确元数据，按实际字节更新固定 REQUESTS_SHA；保留其他 hash、计数和业务键。
- [x] 增加实际 34 描述的 Codec 绑定，以及真实生产插件组合 T03 的 RANGE 拒绝/SINGLE 接收，核对零上游与拒绝时零插入。

### Task 3: 验证、审查与交接

**Files:** 本计划、看板及索引；T06 完成后新增 T07 专属设计与交接。

- [x] 依次执行专属设计四条 Maven 命令并保留日志，执行 preview + `e2e/stock-download-parameters.spec.js` 后关闭本次服务。
- [x] 独立审查设计符合性及代码质量；两轮审查通过，无开放问题。其他39 YAML 原字节、40注册和 `git diff --check` 已核对。
- [x] 记录 T06 outcome 与实际证据，执行 IN_PROGRESS -> COMPLETED。
- [x] 按 Order 选择 T07，使用 designing-task-contracts 完成设计并回填，之后写 next-task 交接、准备 READY；不实施 T07。
- [x] 更新索引，新增文件加入 Git，保留未提交状态。

## Execution record

- 2026-09-11 全文读取 T06 专属设计和交接，核对依赖及总体来源，按用户请求启动；HEAD `dc9ca9e18050c82f227ba1e9995f271798e958d0`，工作树初始干净。
- Ruling: 保持当前分支、不自动提交，遵循仓库与 T06 交接约定；先前完成成果已在当前 HEAD。
- 2026-09-11 T06 最终四条 Maven 门禁退出0：专项449、后端单元867、生产867+4、验收867+4+3；每条生命周期前端170项与构建通过，Chromium3项通过。插件接线与整体设计/质量独立审查通过，无开放问题。结果、命令、环境和证据边界已持久记录于看板 T06。
- 2026-09-11 先记录 T06 COMPLETED，再按 Order 准备 T07。专属设计独立就绪评审通过；全范围纯探针 / 持久片段参数、获准末批完成后的成功汇总、领取前截止溢出均已固定。先回填设计，之后创建并链接 next-task 交接，再执行 T07 NOT_STARTED -> READY。T07 未实施，34生产 RANGE 仍待真实验证。
