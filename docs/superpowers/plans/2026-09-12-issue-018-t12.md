# ISSUE-018-T12 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox syntax for tracking.

**Goal:** 以真实 MySQL、Servlet 与 Chromium 完成后台任务基础设施和交付门禁。
**Architecture:** 复用生产装配与现有故障 IT；测试来源与控制入口仅置于测试类。普通浏览器按独立数据库串行运行，真实账户套件单独发现。
**Tech Stack:** Java 21、Spring Boot、MySQL 8.4.6/Testcontainers、Node 24.15.x、Playwright Chromium。
**Spec:** `docs/task-designs/ISSUE-018-T12-design.md`（完整行为、路径、用例与验收合同）。

## Global Constraints

- 保留现有分支与暂存成果；新增文件加入 Git，不自动提交、合并或发布。
- 单实例、单 worker、手动 retry/resume；生产 40 项注册与 34 项 RANGE NEEDS_VERIFICATION 不变。
- 不用 H2 或 route mock 替代真实生命周期证据；所有源码门禁实际执行，不能以发现列表冒充通过。
- 发布脚本 main/干净源码/HEAD 前置保持；外部账户验证交 T13。

### Task 1: CORS 接收合同

**Files:** `data-plane/tensor-app/src/test/java/com/akkc/tensor/config/ProductionWebConfigurationTest.java`、`data-plane/tensor-app/src/main/java/com/akkc/tensor/config/SpaWebConfiguration.java`。
- [x] 将精确 origin 暴露头断言改为 `X-Request-Id, Location`，运行 ProductionWebConfigurationTest 观察 RED。
- [x] 最小改为 `.exposedHeaders("X-Request-Id", "Location")`；补合法 202 Location 测试并重跑。

### Task 2: 真实应用生命周期

**Files:** `data-plane/tensor-app/src/test/java/com/akkc/tensor/web/DownloadTaskLifecycleIT.java`、`control-plane/e2e/download-task-lifecycle.spec.js`。
- [x] 按专属设计建立生产 Servlet/MySQL/http_test 来源和测试控制入口；先运行缺失 spec 的子进程验证硬失败。
- [x] 完成 flow 两场景、resume 一场景；子进程严格报告验证、真实回执丢失、同库故障快照和最小权限测试。
- [x] 用 LifecycleIT 实际运行，核对 SQL/calls/版本/attempt 与浏览器截图，不依赖模拟 API。

### Task 3: 普通浏览器与隔离环境

**Files:** `control-plane/playwright.config.js`、专属设计列明的七个普通 spec 及 fixtures、`control-plane/e2e/packaged-test-environment.js`、`.test.js`。
- [x] Node 内置测试先覆盖私有映射文件、四 key/五字段、原环境兼容和原子拒绝，再实现最小 helper。
- [x] 配置 ordinary/task-live/tushare-live 三模式及严格参数校验；普通单 worker，三个 mock preview4173、四验收包8080各自空库。
- [x] 迁移旧页面同步断言到接收/查询，保留 SQL、敏感信息、网络白名单和全部原场景；精确 V8 数字同步证据。
- [x] 执行 helper、三模式发现、三个 mock spec，最后以当前验收包执行整个普通套件。

### Task 4: 故障覆盖、脚本与运行手册

**Files:** 专属设计 Files 中的 ControllerIT/RequestBindingTest/core 故障 IT、`scripts/verify-contracts.sh`、`docs/runbook/configuration.md`、`docs/runbook/first-run.md`。
- [x] 对照故障矩阵核对实际方法断言；补同版本并发 resume、未知 HTTP 参数形状和 RANGE 迟到边界的实际缺口。
- [x] 更新精确 47/schema、51/52 表、1051 列等断言及合成拒绝探针，保留发布前置。
- [x] 以实际属性补配置、REFERENCES、接收/恢复/计数说明，核对真实最小权限测试结果。

### Task 5: 六条门禁与交接

**Files:** `docs/verification/ISSUE-018-task-infrastructure.md`、本计划、ISSUE-018 看板与按既定 Order 的 T13 设计/交接。
- [x] 依专属设计顺序运行全 Test/IT、前端单测、生产 clean verify、验收 clean verify、download-tasks spec、普通 e2e；保存日志及报告精确数量。
- [x] 独立审查实现与专属设计，修复后运行受影响检查，核对真实截图与 git diff。
- [x] 写完整证据矩阵，清理仅本次进程/容器/秘密临时文件，新增产物加入 Git。
- [x] 全文复核 T12 设计后记录 COMPLETED；使用 designing-task-contracts 完成并链接 T13 专属设计，再写 next-task 交接并准备 READY，不启动 T13。

## Pause checkpoint

2026-09-12 用户要求将剩余工作交接，T12进入PAUSED，恢复入口为 `docs/task-handoffs/ISSUE-018/ISSUE-018-T12-handoff.md`。G1–G5已通过，G6第四轮106通过/3失败/17未运行，随后query定向1超时失败/10未运行。剩余为扫描等待根因、14列双标签表头与ENUM同步修正的定向验证、完整126项普通门禁、最终证据与审查。本段保留当时未完成事实，checkbox以恢复后的实际结果更新。完成T12后才设计并准备T13，不启动后继实施。

## Resume completion

2026-09-12 从暂停交接第一实施动作恢复；query请求/响应扫描、14列双标签、ENUM同步、tooltip及祖先交集修正完成。最终Lifecycle5/flow2/resume1与G6七文件126项通过，全部六条源码门禁及独立审查关闭，证据/截图/哈希/清理已核对。先记录T12 COMPLETED，再完成T13设计及就绪复核、链接设计、写next-task交接并置READY；未启动T13。原暂存成果保留，本次创建/更新文件加入Git；没有提交、合并、发布、真实Tushare调用或RANGE开放。完整发布脚本仍因main/干净输入/HEAD前置未满足而未运行。
