# ISSUE-018-T13 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox syntax for tracking.

**Goal:** 按既定40项矩阵取得可核验的来源及任务完整性证据，逐项开放成立的RANGE，保留所有未决事实。

**Architecture:** 独立纯证据校验器约束结果索引；测试侧Probe在生产门禁关闭时取得SOURCE证据；账户浏览器harness使用正式task API和独立SQL验证。只有依据与真实SOURCE均成立才改候选策略，再由真实TASK决定开放结论。

**Tech Stack:** Java 21/JUnit/Maven，Node 24/Playwright，MySQL 8.4.6。

**Spec:** docs/task-designs/ISSUE-018-T13-design.md（已全文读取的实施合同，40项逐项细节直接以该表为准）。

## Global Constraints

- 40注册、34股票/6非股票、31原生/3逐日/6SINGLE保持；11 UNKNOWN不得误开放。
- 在现有分支/工作区工作；新增文件加入Git，不自动提交/合并/发布。
- Token只在后端环境；不记录原始响应、凭证或JDBC，缺授权配置不能调用真实来源。
- SOURCE和TASK串行；5000请求/30分钟上限、至少2000ms间隔；一轮失败停止且不自动重试或换样本。
- Maven/npm/浏览器测试串行执行；默认测试不运行Probe/live。

### Task 1: 独立证据矩阵与拒绝校验

**Files:** 新建 control-plane/e2e/tushare-range-evidence.js、tushare-range-evidence.test.js、docs/verification/ISSUE-018-range-acceptance.json。
**Interfaces:** 导出 validateEvidence(index)（不合法抛固定安全错误，合法返回index）、validateCasePlan(plan)（纯输入校验）、safeCaseEvidence(input)（白名单投影）。JSON字段遵循Spec，manifest顺序固定40项；未运行字段null/NOT_RUN。

- [x] 首先建立独立40项矩阵及漏项/UNKNOWN误开放测试，运行 `node --test control-plane/e2e/tushare-range-evidence.test.js` 观察RED。
- [x] 实现最小纯helper，拒绝重复/漏项/额外API、34/6和31/3/6错配、错误参数/日期、缺证据AVAILABLE、失败/未运行PASS、缺构建身份和清理事实，白名单安全投影覆盖秘密输入。
- [x] 创建真实初始索引（全部NOT_RUN；无伪计数），相同命令GREEN并报告精确结果；独立审查。

### Task 2: 测试侧SOURCE探针

**Files:** 新建 data-plane/tensor-plugin-tushare/src/test/java/com/akkc/tensor/plugin/tushare/batch/TushareRangeSourceProbe.java 和 TushareRangeSourceProbeTest.java。
**Interfaces:** 输入/输出严格按Spec私有CASE/EVIDENCE环境变量及JSON合同；复用DatasetDefinitionLoader、TushareProClient、TushareProperties、sourceParameters与openDays；不修改生产代码。

- [x] 对私有文件/目录权限、symlink/owner、未知字段/API、SINGLE/RANGE形状、日期/多股票、授权入口和安全投影写零网络RED测试。
- [x] 实现显式选择的JUnit真实入口、严格私有输入、候选策略纯转换、自然日/交易日安全编排、单一context请求预算及脱敏输出。默认单测不能调用真实入口，缺配置硬失败。
- [x] 用 `mvn -f data-plane/pom.xml -pl tensor-plugin-tushare -am -Dtest=TushareRangeSourceProbeTest -Dsurefire.failIfNoSpecifiedTests=false test` 验证GREEN；再独立审查。

### Task 3: 账户浏览器task与SQL闭环

**Files:** 修改 control-plane/e2e/tushare-live.spec.js；必要的小型纯测试函数继续放Task1 helper并补Node行为测试。
**Interfaces:** 消费Task1校验和Spec case/index；single精确40测试/74真实任务/148查询+fixture2提交3查询；range按实际AVAILABLE及证据驱动。

- [x] 阅读T12设计/基础设施证据和既有harness；以离线行为测试观察旧同步合同拒绝或计数错误。
- [x] 正式202/Location/task/batches/终态/日志路径，逐一路由白名单，SQL前后业务键/股属/摘要，两个股票的历史保留，失败停止与完整清理；保留所有安全边界及既有UI断言。
- [x] Node离线测试、single 40项发现、range精确选择/零选中拒绝；不把发现当执行；独立审查。

### Task 4: 公开依据、真实运行与最终状态

**Files:** 新建 docs/verification/ISSUE-018-range-acceptance.md；更新JSON、必要Policy及测试、runbook、issue索引/看板/交接。
**Interfaces:** 消费前三项实际验证和官方资料；无外部结果时如实记录未运行，保持门禁及T13未完成。

- [x] 逐项读取40官方链接，记录日期、准确引文、语义/上限与未决事实，生成安全索引与代表场景清单。
- [x] 已授权配置及0700/0600私有输入就绪；固定181项SOURCE于2000ms间隔执行，失败停止并保存全量run。SINGLE首次bootstrap零提交失败，导航修复后新空库独立执行至15次任务停止；3轮329个case均已记录，0 AVAILABLE，无自动重试。
- [ ] 取得足够依据及新的有效SOURCE运行后逐项候选策略升级v2、重建包并完成真实SINGLE/RANGE任务验证；本次未达到候选开放条件，策略未变。
- [x] Spec本地五类专项158/158及同生命周期前端468/构建通过。后续导航/SQL修复Node61/61、未来SOURCE投影Probe30/30，均经独立规格/质量复审；真实数据未重跑或回填。本任务未改生产策略/应用源码，未触发六条完整源码门禁；发布脚本未运行。
- [x] 独立最终审查两项Important统一修复，Node67/67且限定复审规格/质量PASS；差异/哈希及历史结果保留核对完成，自有新文件加入Git。先写模板pause交接后记录T13 `IN_PROGRESS -> BLOCKED`，明确新固定取证轮次、稳定源码/新空schema及证据恢复条件；真实验收未满足，母issue保留未解决。
