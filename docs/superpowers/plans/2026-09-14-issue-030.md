# ISSUE-030 候选策略与来源交付 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox syntax for tracking.

**Goal:** 按已完成设计交付30项可供本地验收的候选、272项准确SOURCE绑定和新源码/生产包/验收包。

**Architecture:** 复用现有策略、runner、HTTP及证据消费者；以明确规则和来源映射配置候选。独立测试矩阵同时约束后端能力和受控页面，历史事实保留在唯一索引。

**Tech Stack:** Java21.0.11、Maven3.9.15、Node24.15.0、JUnit/WireMock/Testcontainers、Vitest/Playwright。

**Spec:** `docs/task-designs/ISSUE-030-design.md`（完整设计及唯一30项矩阵）。

## Global Constraints

- 用最小代码实现；保留全部前序未提交成果及当前分支；新增文件加入Git，不commit/push/发布。
- 保留40注册、34RANGE/6SINGLE_ONLY、31原生/3逐日及原日期、参数、字段和业务键。
- 30候选为11 RESPONSE_ONLY、18 ROW_LIMIT、1 CALENDAR_COVERAGE；首次v2；原四v2及其证据/日期保持。
- top_list BJ参照SSE且保留BJ股票；直接trade_cal BSE零来源调用拒绝；margin保留exchange_id三值。
- 正式索引30项仍NEEDS_VERIFICATION；26轮826case928请求历史逐对象不改，不执行真实SOURCE/TASK/SQL。
- Java21/Node24白名单环境，不注入真实账户或业务DB；受控测试使用隔离MySQL与测试包。
- 272来源映射准确消费母issue七组交付表268项及新四mainbz；完整run/case引用，不按同参数first-match选择。

## Task 1: 候选生产配置、独立能力和页面预期

**Files:** 修改`TushareBatchPolicies.java`及同目录测试`TushareBatchPoliciesTest.java`、`TushareBatchDownloadTest.java`、必要的`TushareTradeCalendarTest.java`；修改`tensor-app/.../TushareBatchAvailabilityTest.java`、`control-plane/e2e/ui-redesign.fixtures.js`、`tushare-metadata.spec.js`及受影响的受控fixture/spec。

**Interfaces:** 消费设计第1节逐项矩阵、现有`BatchDownloadDescriptor`、`BatchAssessment`、HTTP extraction；产出明确候选v2能力及原保存摘要语义，供Task2来源引用和Task3两包验收。

- [x] 先在PoliciesTest与AvailabilityTest显式写出全部34项规则/版本/阈值/规划/日期/参数预期，十一项不可拆；运行定向Maven观察缺少候选的RED。
- [x] 用明确30项依据映射实现`sourceVerified`，每项包含决定（适用）、逐接口报告、官方URL及完整有效SOURCE引用；文案反映已采用工程阈值/历史查询/允许不完整，实际核对日期2026-09-14。保留四旧记录。
- [x] 增加`.BJ -> SSE`日历参照；测试SSE日历后仍按原BJ证券请求top_list，以及直接BSE零调用拒绝、三种margin exchange_id。
- [x] 测试十一项一叶计划、合法空/非空、结构/日期/股票错误；严格限量等号及最小满额、完整日历不被响应采集放宽。
- [x] 同步独立前端RANGE_EXPECTATIONS及能力fixture；metadata按三种规则分支断言，任务fixture从明确矩阵获得摘要，历史UNKNOWN和严格fixture显式保留。
- [x] 运行`python3 /private/tmp/issue030-control/run-tests.py task1-green.log -Dtest=TushareBatchPoliciesTest,TushareBatchDownloadTest,TushareTradeCalendarTest,TushareBatchAvailabilityTest -Dsurefire.failIfNoSpecifiedTests=false test`；记录真实计数/RED/GREEN与受影响检查，完成独立规格/质量审查。

## Task 2: 正式候选引用与272项精确SOURCE绑定

**Files:** 修改`docs/verification/ISSUE-018-range-acceptance.json`、对应`.md`、`control-plane/e2e/tushare-range-evidence.test.js`；必要时新增小型可重建候选清单模块和对应测试。私有输入与摘要置于`/private/tmp/issue030-control/`，0700/0600。

**Interfaces:** 消费Task1候选及母issue七组精确输入、已有`validateEvidence/validateCasePlan/selectTaskCases`；产出schema2候选引用、272完整绑定、可冻结输入，不产生TASK结果。

- [x] 从母issue精确ID表构造独立输入断言，固定组数28/12/38/35/35/33/87及新mainbz4，先运行Node观察旧候选缺少规则/引用的RED。
- [x] 用准确ID从唯一索引取原params/dateAxis，选择清洁有效SOURCE及适用旧SINGLE TASK；旧空/失败/四满额/BSE负例仅移出当前候选cases，runs逐对象保持。
- [x] 为30项填入采用规则、evidenceRefs/decisionRef及v2，重算所选sourceStatus/taskStatus，清除已解决合同缺口，保留RANGE_TASK_NOT_RUN及NEEDS_VERIFICATION；四AVAILABLE完整对象保持。
- [x] 每项计划包含完整规则依据和恰一个完整SOURCE run/case引用；保存sourceBindings并校验272项。mainbz原12与新4精确区分；错误run、歧义、旧满额、未引用、响应无身份拒绝；SOURCE不能单独使正式AVAILABLE。
- [x] 运行`node --test control-plane/e2e/tushare-range-evidence.test.js`及新增定向测试，输出历史保全、绑定与输入hash机器摘要；独立规格/质量审查。

## Task 3: 完整隔离构建、受控验收与交接

**Files:** 新增`docs/verification/ISSUE-030-range-candidate-policies.md`；更新`ISSUE-018-T14-runs.md`、issue问题/索引及权威子看板；验收后准备`ISSUE-031-design.md`与`ISSUE-031-handoff.md`。

**Interfaces:** 消费前两项通过审查的源码、矩阵和固定输入；产出逐文件可复核隔离checkout、生产/验收JAR/manifest/examples SHA-256及真实测试结果。

- [x] 运行Node证据、前端单测与相关回归；修复有证据支持的问题后才冻结。
- [x] 复制全部Git管理的当前源码到私有隔离checkout，逐文件hash核对；冻结HEAD+完整相关diff及全文件snapshot。使用Java21/Node24运行`mvn -o -f data-plane/pom.xml -Pacceptance verify`，保存所有JUnit XML真实总数和两包摘要。
- [x] 运行受控`ui-redesign.spec.js`、`download-tasks.spec.js`与packaged-test-environment入口的`tushare-metadata.spec.js`；核对响应采集表单/非空/空列表详情及历史说明；不选择tushare-live。
- [x] 完成整体独立审查、`git diff --check`和`git diff --cached --check`，确认历史26轮826case不变、源码/两包/输入稳定、受控资源清理。
- [x] 根据真实结果更新验收与构建登记、ISSUE-030 IN_PROGRESS→COMPLETED，再完成并先链接ISSUE-031专属设计，写后继交接并准备READY；母任务保持IN_PROGRESS，后继不实施。
