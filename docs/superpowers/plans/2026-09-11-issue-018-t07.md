# ISSUE-018-T07 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans or superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox syntax for tracking.

**Goal:** 按持久计划串行执行任务，在完整性、预算和许可边界内原子提交数据与成功状态。

**Architecture:** 新增纯日期工具与同步 DownloadTaskRunner，复用真实仓储和 BatchCommitService。T03 仅增加包内执行定义复用，T04 增加兼容停止重载。来源与适配均不进入数据库事务。

**Tech Stack:** Java 21、Spring JDBC、JUnit 5、Testcontainers MySQL 8.4.6。

**Spec:** `docs/task-designs/ISSUE-018-T07-design.md`（完整接口、错误矩阵、边界与测试合同）。

## Global Constraints

- 当前工作分支保留，T06 暂存成果保留；新增文件加入 Git，不自动提交。
- 默认 true / 36600 天 / 10000 节点 / 5000 预约 / 30 分钟 / 1000000 成功来源行。
- 不修改公共插件合同、仓储表面、迁移、40 项 YAML、前端、生产来源开放状态或依赖。
- 同步调用实际退出前持有占用；停止后不提交迟到响应，已获准事务允许共同完成。
- T08 生产协调、retry/resume 和 T09 HTTP 不在实施范围。

### Task 1: 日期算法

Files: core `download/task/DateRangePlanner.java` 与对应 `DateRangePlannerTest.java`。

- [x] 写闰日、跨年闭区间二分和端点枚举失败测试；运行 `mvn -f data-plane/pom.xml -pl tensor-core -am -Dtest=DateRangePlannerTest -Dsurefire.failIfNoSpecifiedTests=false test`，观察缺失类。
- [x] 实现不可变结果；二分 `middle=start.plusDays(DAYS.between(start,end)/2)`，右端从 middle+1 开始；枚举在等于 end 后退出。
- [x] 验证单点拒绝、两天、奇偶数、MIN/MAX 与 MAX 单点，运行同一命令通过。

### Task 2: 执行定义与提交停止边界

Files: core `DownloadTaskService.java`、`DownloadTaskServiceTest.java`、`BatchCommitService.java`、`BatchCommitServiceIT.java`。

- [x] 写包内 `executionDefinition(DownloadTask)`、定义变化优先和外层事务拒绝失败用例。
- [x] 提取 current→hash→normalize 共用校验，返回 `ExecutionDefinition(plugin,adapter,dataset,range)`，SINGLE range=null。
- [x] 写五参数 commit 预先停止、等待锁停止及 beforeWrite 获准后停止的失败用例。
- [x] 四参数 commit 委托五参数并传 `() -> false`；仅预检查和 beforeWrite 原许可/截止检查之后检查 stop，不在 afterWrite 检查。
- [x] 运行 ServiceTest、BatchCommitServiceIT、PersistenceServiceIT；真实 MySQL 验证停止零写入和获准事务共同成功。此停止重载子项可独立于 runner 编写，编辑文件互不重叠。

### Task 3: 串行执行与故障语义

Files: core `DownloadTaskRunner.java`、`DownloadTaskRunnerTest.java`、`DownloadTaskRunnerIT.java`；fixture `FixtureDownloadTaskRunnerTest.java`。

- [x] 先写真实 T03 + 受控来源的 SINGLE 空批 commit 和三批第二失败后第三成功用例，观察 runner 缺失。
- [x] 实现设计固定 Settings / RunResult / Disposition / runNext；占用 finally 释放，事务外领取并建立唯一 context，预约由仓储原子执行。
- [x] 三类计划先完整核对再 savePlan；已有 planReady 跳过规划，sourceParams 使用持久值。加入非法计划与快照不被覆盖测试。
- [x] 按设计顺序验证控制、当前定义、envelope、assessment，再 split 或 adapt/commit；满额父不适配，空批仍提交。
- [x] 明确区分存储不确定性与批次失败；参数化覆盖全部 ErrorCode 的继续/停止/恢复矩阵，finish 从 snapshot 汇总。
- [x] 添加默认预算等号/+1、请求上下文、定义/可用性变化、晚到旧调用、重入/并发与新轮次测试。
- [x] MySQL 证明三批部分失败、二分原子树、触发器回滚/失败标记故障、提交后回执丢失、累计计数、新旧许可和实际退出后恢复。
- [x] fixture 使用真实旧插件验证 SUCCESS / EMPTY / SOURCE_FAILURE / TYPE_FAILURE，普通 SINGLE 每次预约一次。

### Task 4: 验证、审查与任务交接

- [x] 执行专属设计 Tests 节的显式 MySQL 专项命令；全部选定 IT 实际运行，失败/错误/跳过为 0。
- [x] 执行设计的全单元、`clean verify`、`-Pacceptance clean verify`；逐条保存日志与实际数量。
- [x] 独立审查 T07 的设计符合性和代码质量；发现有可复现缺陷时补测试、最小修复、复核。
- [x] `git diff --check`、新增文件 Git 暂存及范围检查通过后，先写 T07 COMPLETED 和 outcome/验证证据。
- [x] 按 Order 选 T08，使用 designing-task-contracts 完成专属设计并回填，再写 next-task 交接并准备 READY；不启动 T08 实现。

## Execution Record

- 2026-09-11：设计与交接全文读取；T07 已启动。Colima 原停止，启动现有实例成功，未删除用户容器。

- 2026-09-12：实现及独立审查完成；149项专项、926项全单元、生产926+4/验收926+4+3通过，前端170项及构建通过。先记录T07 COMPLETED，开始后继设计与交接准备。
- 2026-09-12：T08 专属设计独立复核通过，生命周期 FAULTED 与控制前动态 readiness 两项设计缺口修正并复核关闭。按序回填设计、创建并链接 next-task 交接、记录 T08 READY；模板、18 项既有输入路径及设计链接核对通过，索引同步。T08 实现未启动，当前分支及 T06 / T07 暂存成果保留，无提交。
