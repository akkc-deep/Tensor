# ISSUE-028 Implementation Plan

> **For agentic workers:** 使用superpowers:subagent-driven-development实施，测试先行，独立审查后交付。

**Goal:** 贯通持久采集摘要、严格HTTP合同及下载/任务页面说明。

**Architecture:** Controller直接传入policySummary，正式schema与前端严格解析同步；页面仅以持久extraction解释历史任务。

**Tech Stack:** Java21/Spring/JUnit/MySQL Testcontainers，JSON Schema/OpenAPI，Node24/Vue/Vitest/Playwright。

**Spec:** [ISSUE-028设计](../../task-designs/ISSUE-028-design.md)。

## Global Constraints

保留现有暂存成果和分支，不提交、推送或发布；新增文件Git纳管。生产4 AVAILABLE/30 NEEDS_VERIFICATION/6 SINGLE_ONLY与唯一真实验收索引保持，不执行真实SOURCE/TASK。不引入确认步骤，SINGLE和严格规则原语义保持。

### Task 1: 后端与正式合同

**Files:** DownloadTaskResponse.java、DownloadTaskController.java及相关测试；docs/contracts/download-task.schema.json、download-task-examples.json、openapi-v1.yaml。

**Interfaces:** 消费DownloadTaskService.policySummary(DownloadTask)，输出必需extraction：SINGLE null，RANGE精确{policyVersion,ruleKind}。

- [x] ContractTest先覆盖RESPONSE_ONLY合法能力和缺/非法extraction，运行观察RED。
- [x] DTO使用from(snapshot, controls, summary)，Controller以同一task取得controls和summary；列表沿用详情转换。
- [x] schema增加RESPONSE_ONLY无阈值/原生/不可拆约束（含NEEDS_VERIFICATION），TaskPolicySummary精确字段与mode约束；保留全部原examples并补准确摘要和新规则非空/空/历史任务示例。OpenAPI同步来源和成功语义。
- [x] HTTP覆盖SINGLE、原严格/UNKNOWN、新规则及当前能力变化后历史不变，运行合同/控制器/集成/准入相关测试GREEN。

### Task 2: 严格前端和页面

**Files:** downloadTaskDtos.js及spec，DownloadView、DownloadTaskView、DownloadTaskList及spec；所有受影响API/composable/E2E任务fixture。必要时新增小型纯文案工具。

**Interfaces:** 精确冻结extraction，不读当前capabilities解释历史；所有计数维持BigInt。

- [x] DTO先覆盖新能力合法/非法、RANGE缺摘要及SINGLE非null等，运行RED。
- [x] 实现严格解析与所有fixture同步；RANGE冻结二字段摘要、拒绝空版本/未知枚举/多键/缺键。
- [x] 页面先补RESPONSE_ONLY全部状态和sourceRows零/非零、UNKNOWN历史、严格及SINGLE、快照更新测试，运行RED。
- [x] 表单提示“按所选日期区间采集本次接口返回的记录。数据完整性未确认，可能存在上游截断。”；列表/详情持续提示“数据完整性未确认，可能存在上游截断”。仅SUCCEEDED显示“返回记录已采集”或“本次请求未返回记录”；UNKNOWN显示“数据完整性未确认”。展示保存的规则/版本。
- [x] npm test GREEN，受控download-tasks E2E覆盖提交前及终态列表/详情，保持原布局与人工恢复。

### Task 3: 验证与交接

- [x] 使用白名单Java21/Node24环境与已观察Colima socket运行设计中的专项后端、npm test、acceptance verify及受控E2E；汇总实际数量/失败/跳过。
- [x] 独立审查规格与代码，修复并按影响补验，核对生产注册与真实索引保持。
- [x] 记录ISSUE-028验收、权威看板完成及ISSUE-029后继设计/交接，不开始后继实现。
- [x] 新文件加入Git，git diff --check及git diff --cached --check通过。
