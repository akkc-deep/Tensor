# ISSUE-029 Implementation Plan

**Goal:** 严格验证响应采集与指定SOURCE身份，取得四项主营业务拆分来源。

**Spec:** [已完成设计](../../task-designs/ISSUE-029-design.md)，共享设计第4节及mainbz约定。

## Global Constraints

在用户授权的当前feat/download-by-date-range分支及未提交成果上实施，不commit/push/发布。最小代码，新增文件加入Git。旧25轮822case逐对象保持，生产注册/版本保持。仅四项固定新SOURCE，不执行TASK/SQL，不自动重试。普通回归使用无账户/业务DB的白名单Java21/Node24环境；真实SOURCE先固定完整隔离源码、两包、输入和预算。

### Task 1: 证据消费者与harness

**Files:** control-plane/e2e/tushare-range-evidence.js、tushare-range-evidence.test.js、tushare-live.spec.js。

按专属设计Approach 1～3和Tests实现。第一动作添加schema1/2 RESPONSE_ONLY及同参数两个清洁SOURCE的指定身份/歧义拒绝测试，观察RED。实现schema兼容、精确身份选择与共享sourceBindings；响应采集TASK预期由采用合同生成，严格检查单请求整段单成功叶子、SQL、能力/持久摘要/版本和页面。保持旧严格规则及白名单，不改正式索引或生产。运行完整Node局部回归并独立审查。

### Task 2: 测试侧mainbz递归拆分

**Files:** TushareRangeSourceProbe.java、TushareRangeSourceProbeTest.java；tensor-plugin-tushare/pom.xml仅增加tensor-core的test依赖，以直接复用DateRangePlanner.split，不改变生产依赖。

按专属设计Approach 4和Tests测试先行。仅fina_mainbz RANGE按已批准100阈值递归DateRangePlanner.split，先左后右，父不投影/不计成功行，单日满额失败；保留完整真实树与预算/停止/无重试规则。其他API与SINGLE保持。运行Probe及相关回归并独立审查。

### Task 3: 隔离构建和四项真实SOURCE

按专属设计Approach 5固定UTC runId、四case、原参数和最坏树预算。消费者通过后升级正式schema2，保留全部旧对象。冻结全部当前源码到隔离checkout并逐文件核对，执行acceptance构建和Node检查，冻结生产/验收包及manifest/examples。独立审查通过后用受控既有wrapper执行四SOURCE；失败即停，不重试。核对树、叶子阈值、非空、退出/清理/哈希，实际结果仅追加唯一索引/Markdown/登记。结果未满足时如实保留缺口。

### Task 4: 最终审查与交付

核对旧对象与生产保持、原12来源/新四来源精确绑定及全部实际结果。记录本issue验收和权威状态；满足后完成并按既有看板准备ISSUE-030设计/交接。新增文件Git纳管，两种diff check通过，不自动commit/push。

## 实际完成记录

Task1/2实现、失败测试及独立修复复审完成；Node98、Probe67、相关154、隔离acceptance后端/包1132和前端524通过。Task3四项真实SOURCE均PASS、42请求，冻结身份稳定；旧25轮822case及40个interface保持，16项精确绑定通过。Task4整体代码与实际证据终审PASS，ISSUE-029已完成；具体事实见[验收](../../verification/ISSUE-029-range-evidence-and-split-source.md)，后继仅准备设计/交接。
