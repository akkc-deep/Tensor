# ISSUE-024 Implementation Plan

> **For agentic workers:** 使用superpowers:executing-plans顺序实施，superpowers:requesting-code-review独立复审。

**Goal:** 补足三接口历史SOURCE及ISSUE-026输入，准确解决历史承诺缺口。

**Architecture:** 复用现有Probe/私有wrapper/唯一索引，仅增加测试侧原业务键与期限费率安全计数。

**Tech Stack:** Java21/JUnit、Maven、Node24、JSON/Markdown。

**Spec:** [专属设计](../../task-designs/ISSUE-024-design.md)。

## Global Constraints

保留20轮/648case、生产策略及原业务键；所有真实请求预登记、至少2000ms、无自动重试。新文件Git纳管，不自动提交。历史支持采用口径按2026-09-14用户明确同意的方案A执行；未知保证仍保留。

### Task 1: 安全历史投影

- [x] 在`TushareRangeSourceProbeTest.java`构造融资同日两ob与重复等值键、证券汇总股日键、明细同日不同期限和同期限不同费率，断言计数及脱敏；补非法键、空/失败/SINGLE隔离。
- [x] 运行专属设计首条命令，确认仅缺计数RED。
- [x] `TushareRangeSourceProbe.java`新增`SlbProjection`，由既有`Projection.observe/review`在三接口RANGE且成功观察时调用；按原键规范化十进制，只输出计数和摘要。
- [x] 首条及第二条命令GREEN，独立复审投影、原键与字段隔离。

### Task 2: 固定历史SOURCE

- [x] 将公开引用、固定run/case/params及私有清单哈希写入T14登记。整体窗口、官网日、停业前后和明细较长窗口分别标明调查目的。
- [x] 完整Git管理源码复制独立clone逐文件校验；隔离acceptance verify、Node78项通过，固定源码/两包身份后运行wrapper。
- [x] 按实际日期预登记缺失非空端点的新轮；保留首轮所有空/失败，核对每轮退出、清理和实际计数。
- [x] 追加唯一索引，形成历史合同准确缺口和必要的具体用户采用方案；不自行改支持范围。

### Task 3: 交付与验收

- [x] 将全部非空清洁RANGE候选以独立TASK身份和精确SOURCE绑定交ISSUE-026；离线`selectTaskCases`正负例检查。
- [x] 同步三接口状态/缺口及Node独立预期；核对20旧轮/其他37接口、40行报告、源码/两包及原对象稳定。
- [x] Node及适用回归、链接和diff检查通过；最终独立复审无剩余发现。
- [x] 用户2026-09-14明确采用方案A后，按限定口径复核全部关闭条件，决定后验证和独立复审通过，更新COMPLETED。
