# ISSUE-022 Implementation Plan

> **For agentic workers:** 使用superpowers:executing-plans按下列步骤执行；独立复审使用superpowers:requesting-code-review。

**Goal:** 补足五接口真实日期语义和边界证据，交付ISSUE-026。

**Architecture:** 复用测试Probe、私有SOURCE wrapper及唯一验收索引，仅追加两个接口的整数投影。生产策略保持原样。

**Tech Stack:** Java21/JUnit、Maven、Node24、JSON/Markdown。

## Global Constraints

沿用[专属设计](../../task-designs/ISSUE-022-design.md)和T13/T14；旧15轮/570case保留，真实调用预登记、串行至少2000ms，无自动重试，新增文件加入Git，不自动提交。

### Task 1: 安全日期对照

- [x] 在`TushareRangeSourceProbeTest.java`增加两接口合成行测试：主轴在20260803～04，副轴分别为范围外、另一端、同日、null、20260230；断言valid=3/unavailable=2/different=2/outside=1。另测空/失败/未执行/SINGLE范围隔离和错误主轴拒绝。
- [x] 执行`mvn -o -f data-plane/pom.xml -pl tensor-plugin-tushare -am -Dtest=TushareRangeSourceProbeTest -Dsurefire.failIfNoSpecifiedTests=false test`，记录缺指标RED。
- [x] 在`TushareRangeSourceProbe.java`的Projection启用两接口，new_share列名选ipo_date/issue_date，holdernumber沿用ann_date/end_date，严格解析及整数输出复用现有路径。
- [x] 同命令及Policies/BatchDownload/TradeCalendar相关回归GREEN，更新T13限定投影说明。

### Task 2: 固定SOURCE

- [x] 将精确caseId、params、日期轴、公开/旧观察依据、预算和私有哈希写入`ISSUE-018-T14-runs.md`；完成专属设计自查。
- [x] 完整隔离源码逐文件核对，acceptance verify及Node证据校验通过，登记实际源码/两包身份。
- [x] 受限wrapper只执行登记清单，保留安全原对象、退出和清理；第二轮仅消费第一轮明确日期作为边界依据，先登记再执行。
- [x] 按完整日历核对周期最后开市日、原始行数和两日期整数，追加唯一JSON，更新五接口来源结论与40行报告。

### Task 3: 交付与关闭

- [x] 在`ISSUE-026-range-task-final-acceptance.md`写精确SOURCE到新TASK身份/参数绑定，使用selectTaskCases离线核对，并验证空/失败拒绝。
- [x] 检查旧15轮及其他接口不变、新run与私有原对象一致、总数和摘要一致；同步`control-plane/e2e/tushare-range-evidence.test.js`的当前引用/状态预期及五接口禁止提前开放检查，Node回归、独立复审、diff检查通过。
- [x] 逐条对照专属设计关闭条件，更新ISSUE-022、问题索引和followups看板；新增文件Git纳管，下一issue保持未开始。
