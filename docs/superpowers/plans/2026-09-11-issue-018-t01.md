# ISSUE-018-T01 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** 建立可选批量合同、严格区间绑定和完整错误映射。

**Architecture:** plugin-api 仅新增纯合同；app 复用现有形状匹配和校验；后续任务消费显式描述入口。现有源码工作区继续使用，保留用户暂存内容。

**Tech Stack:** Java 21、Maven、JUnit、Spring MVC、Vue / Vitest。

**Spec:** `docs/task-designs/ISSUE-018-T01-design.md`

## Global Constraints

- 用最小代码实现。将创建的文件加入 Git 版本控制。
- 不改 DataSourcePlugin.download，不改 40 份 YAML，不实现任务存储 / 执行或新任务 HTTP。
- 日期输入 YYYYMMDD；四形状各唯一 Codec；UNKNOWN 不能 AVAILABLE。
- 测试结果写入看板证据；完成后只准备 T02，不执行 T02。

### Task 1: 插件可选合同

- [x] 在 plugin-api 新增 DateRange / BatchDownloadDescriptor 行为测试，覆盖设计全部构造不变量；运行 `mvn -f data-plane/pom.xml -pl tensor-plugin-api test` 观察缺失合同失败。
- [x] 按专属设计加入 BatchDownloadSupport、download/batch 下 5 个文件；旧插件无需修改。
- [x] 再运行 plugin-api 测试并核对依赖未变。

### Task 2: 参数与错误集成

- [x] 扩展 DownloadParameterResolverTest 的四形状绑定与非法值矩阵；将重复字段覆盖例改为严格拒绝；前端 api.spec.js 添加十项错误规则并观察失败。
- [x] 按专属设计新增两个嵌套参数类型、两个 Codec、显式 ApiDescriptor 入口、JSON 重复检测。
- [x] 补十项 ErrorCode、SourceException 的范围错误、HTTP / 前端映射和错误文档，更新相应枚举测试。执行专属设计列出的后端、前端测试及构建。

### Task 3: 复核与交接

- [x] 按 T01 专属设计检查差异、验证结果与范围；完成独立代码复核，无需修复项。
- [x] 重读 T01 设计，记录 T01 COMPLETED 和具体证据。
- [x] 完成 T02 专属设计并链接；按模板创建 T02 交接，链接后变更为 READY。
- [x] 新文件加入 Git，检查 diff 与看板引用。保留工作区供用户审阅，不自动推送或合并。
