# ISSUE-023：稀疏事件与记录修订来源验证

## Goal and scope

用户于2026-09-13要求“完成issue23”。沿用[T13](ISSUE-018-T13-design.md)及[T14](ISSUE-018-T14-design.md)已授权的取证合同，负责`block_trade`、`disclosure_date`、`stk_holdertrade`、`pledge_detail`的事件语义、整段和边界SOURCE，交付ISSUE-026的TASK/SQL输入。旧17轮/609case保留；本issue不修改生产参数、列、业务键、阈值、版本或准入。

## Evidence and approach

1. 先读取官网161/162/175/111原文与旧安全摘要，再固定有事件依据的股票和日期。block_trade、disclosure_date、stk_holdertrade两只基准股票仍为000001.SZ、600000.SH。用户在首轮000001.SZ质押快照再次为空后明确同意调整：仅pledge_detail使用000014.SZ和600000.SH完成非空整段/边界；000001.SZ空样本保留。其他官网股票仅为代表事件补充，不替代其余接口基准股票。只基于公开事件或已取得的实际日期登记新轮，空/失败保留，不循环换样本求成功。
2. 复用现有Probe与私有wrapper。测试侧RANGE请求`stk_holdertrade`额外选择官网的`begin_date/close_date`，`disclosure_date`额外选择`modify_date`；先严格校验扩展Envelope，再剥离这几个辅助列交原策略校验。计数观察使用同一份完整响应，不追加账户请求、不修改YAML或生产校验。SINGLE及其他接口使用原列；缺列/错序/非法主轴仍失败。
3. 最小安全投影：大宗交易仅内存按原六列业务键和股票/交易日分组，输出有效/不可用键行数、不同键数、同股同日多键组数和不同买卖方组合数量，保留重复原始行计数。数字键按十进制规范化，不将金额、买卖方名称或原始行写入证据。
4. 股东增减持分别比较ann_date与begin_date/close_date，质押分别比较ann_date与start_date/end_date/release_date，按同一行输出有效/不可比较/不同/公告在内而业务日在外的整数；缺失或非法副轴计不可比较，不当成范围外。质押SINGLE仅补合法公告日期集合，为已有快照的固定边界取样提供依据，不宣称快照是全历史。
5. 披露计划按原`[ts_code,end_date]`键统计，输出报告期集合、有效/不可用键数、不同键数及键/五列记录的SHA-256安全摘要；统计同键不同记录、公告与报告期区别、预计/实际日期区别，以及modify_date存在/可解析日期情况。自然日逐日请求保留每一叶子；修正记录或重复读取的一致摘要只证明该样本的修订线索/当次复查，不能宣称已经观察到跨时点更新或历史每版可回放。实际入库更新由ISSUE-026验收。
6. 投影只消费已通过主轴/归属验证的响应；有效+不可比较等于对应观察行数。成功空响应输出0，尚无成功响应或未执行不输出，部分失败只保留已成功叶子的观察并维持失败。测试使用合成值验证字段隔离、同一行关系、无效值与脱敏，再执行相关回归。
7. 新文件先Git纳管。完整Git管理源码（含已暂存和未提交成果）复制到独立clone逐文件核对，普通测试移除账户及DB环境，隔离`mvn -o -f data-plane/pom.xml -Pacceptance verify`与Node证据校验通过。真实执行前在T14运行登记写清runId/caseId/params/dateAxis/事件依据/预算/私有清单哈希及源码/两包身份；0700目录、0600清单、HTTPS、至少2000ms间隔、30分钟/5000次上限、35分钟外层超时，失败停轮、无自动重试，校验退出和清理。
8. 新证据只追加唯一JSON及T14运行登记；其他36接口和旧run对象保持。按实测结论更新四接口当前缺口、40行报告及issue状态，向ISSUE-026交付精确SOURCE绑定与新TASK身份。来源不足则如实保持未完成，不以文档或合成测试代替真实证据。

## Verification and acceptance

- 非空大宗交易同股同日多笔及买卖方区分，原业务键不变。
- 非空披露计划能对应最新公告轴、报告期和修订线索，同键复查有安全摘要；实际数据库更新交ISSUE-026。
- 两类股东事件有同一行公告日与业务日期对照；副轴不可比较值不误算。
- 四接口两股票固定整段与边界来源成立，完整性阈值1000/6000/3000/1000按原合同判定，不用小样本推导新规则。
- 新run/输入/构建/清理身份、历史对象、Node消费者正负例及相关Maven回归通过；最终复审后才关闭本issue，后继及母issue不提前完成。

## 质押非空样本决定（2026-09-13）

用户明确回复“同意调整质押非空样本（推荐）”。对应具体问题是保留000001.SZ固定快照0行，使用第一轮真实非空的000014.SZ与600000.SH完成该接口非空整段/边界。此决定仅修订质押验收股票，不改变生产支持范围、参数、日期轴、业务键、完整性阈值或历史承诺；不能将000001.SZ空结果改为PASS或断言其不存在任何历史事件。其余三接口继续原两股票，实际TASK/SQL仍由ISSUE-026完成。

## 完成证据（2026-09-13）

三轮39case/131请求，38非空PASS与1质押空SINGLE保留，三个exit0/cleanup PASS。四接口事件/日期语义、两股票整段及边界成立，质押股票按明确用户决定调整；披露当前五列摘要匹配公开修改后预约且原键重复复查稳定，modify_date为空，不冒称历史每版或本轮跨时点更新。35项精确TASK候选交ISSUE-026，10旧空/失败RANGE与空SINGLE拒绝。

184项相关Maven、隔离1083后端/包+468前端、Node78/78通过，独立最终复审无剩余发现；真实原对象/输入/源码/两包/历史17轮/其他36接口/40行报告/204链接及锚点核对通过。全部关闭条件成立，ISSUE-023完成；生产v1及TASK/SQL、母issue/T13/T14保持后续验收，详见[最终验收](../verification/ISSUE-018-T14-runs.md#issue-023-最终验收)。
